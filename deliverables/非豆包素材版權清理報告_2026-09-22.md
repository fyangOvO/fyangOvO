# 七傳說 · 非豆包素材版權清理報告

> 日期：2026-09-22
> 範圍：整個專案 `D:\七傳說`（遊戲本體 `game/`、交付目錄 `deliverables/`、工具工作目錄 `.workbuddy-ai/`）
> 指令：刪除所有「非豆包生成的素材」，刪除/重寫專門導入這些素材的代碼與導入邏輯，讓遊戲只跑豆包原創素材；直接刪、不回滾；刪除導致的缺圖/缺功能記錄後續補。
> 風格基線：嚴謹 2D 像素風（DNF 暗黑地下城手感），嚴禁厚塗/寫實/插畫。

---

## 一、結論（先看這裡）

- 遊戲本體內的第三方整合包 `game/assets/dnf/`（**1819 檔 / 8.44 MB**，含拳皇、街霸、漫威 vs 卡普空、植物大戰殭屍、Minecraft、DNF 等受版權素材）已**物理刪除**。
- 專門導入/抓取第三方素材的 **20 個工具**、主選單第三方英雄立繪與武器展示欄代碼、第三方 UI 貼圖表全部**刪除或重寫**。
- 交付目錄與工具工作目錄的第三方備份、選型圖集、抓取瀏覽器快取、開發抓圖一併清除（**約 1283 檔 / 325 MB**）。
- `godot --headless --import` 成功；**全量回歸 54 個腳本 + self_check 111 項全部全綠（77.2s，FAIL=0）**。
- 全專案圖片檔名第三方關鍵詞掃描**歸零**；遊戲內圖片（1827 張）全部為豆包素材，`assets/` 之外無散落圖片。
- 遊戲可正常運行；因刪除產生的缺圖/缺功能（主選單立繪、武器展示欄、玩家其餘方向動畫等）見第六節，已記錄待補，**本輪不製作**。

---

## 二、刪除明細

### 2.1 遊戲內第三方素材（`game/`）

| 路徑 | 內容 | 規模 |
|---|---|---:|
| `game/assets/dnf/` | raw 原始下載 518、normalized 規整 1052、characters 194、fx 18、ui 26、_manifest（10 JSON + README） | **1819 檔 / 8.44 MB** |
| `game/dnf_preview.png` + `.import` | 第三方素材預覽抓圖 | 2 |
| `.godot/imported/dnf_preview.png-*.ctex/.md5` | 失效導入快取 | 2 |
| `deliverables/gstack/`（內 17 檔） | 16 張開發抓圖 PNG + `_contact.py`（空目錄保留） | 17 |

> `data/` 配置本就不引用 dnf（裝備 icon_path 指 `icons/equipment/`，怪物 sprite_path 指 `sprites/enemies`），故刪除不影響資料層。

### 2.2 第三方抓取/導入工具（20 個）

- 5 個 `.gd` 三件套（含 `.tscn`）：`capture_dnf_pc`、`dnf_assets_capture`、`dnf_assets_preview`、`dnf_pc_proof`、`verify_dnf_ui`。
- 15 個 `.py`：`dnf_asset_pipeline`、`dnf_build_pc`、`dnf_main_menu_assets`、`dnf_normalize`、`dnf_stage_in`、`dnf_weapon_icons`、`aigei_batch_dl`（含 r4/r5/r6/r6_retry）、`aigei_dl_fx`、`aigei_extract_sets`、`aigei_open`、`aigei_probe`。
- 回歸腳本數由 55 降為 **54**（`verify_dnf_ui` 已刪）。

### 2.3 交付目錄 / 工具工作目錄殘留（本輪）

| 路徑 | 性質 | 規模 |
|---|---|---:|
| `deliverables/_quarantine_2026-09-22/bakimg/` | 第三方素材備份（dnf_album / dnf_char / dnf_monster / dnf_map / dnf_tileset / dnf_ui / dnf_skill，含 64 gif、bgm） | 779 檔 / 6.34 MB |
| `…/dot_vu/_disabled_old_normalized/` | 舊第三方 normalized 幀（boss_forest/ghoul/golem/player_alt_vx 等 + manifest） | 119 檔 / 0.37 MB |
| `…/dot_vu/sheets/` | 第三方選型圖集（拳皇/街霸/漫威卡普空/PVZ/三國志/RPG Maker/山海經/DNF 等拼合圖） | 55 檔 / 2.33 MB |
| `…/dot_vu/`（根） | 含第三方時期畫面的開發/QA/mock 截圖 | 47 張 |
| `…/dot_vu/` 下載清單 | `dnf_raw_catalog.json`、`icons_urls.json`、`icons_raw.txt`、`dl_urls.txt` | 4 |
| `.workbuddy-ai/aigei-profile/` | aigei 抓取用瀏覽器設定檔與快取 | 227 檔 / **312.63 MB** |
| `.workbuddy-ai/tmp/`、`…/trash/` | 第三方封面裁切（hero_*/z_*）與開發截圖、廢棄按鈕 | 38 + 14 張 |

本輪合計約 **1283 檔 / 325 MB**。

---

## 三、代碼改寫（只移除第三方路徑與符號，保留通用幀解析函數）

| 檔案 | 改動 |
|---|---|
| `scripts/enemies/enemy_base.gd` | 刪 `DNF_NORM_ROOT`、`dnf_load_set()`、`dnf_read_manifest()`；解析鏈第③級改 `_dnf_empty(id)` 空集；`dnf_load_dir()` 去 `use_manifest`（現兩參）、meta 恆空、canvas 實測、fps 恆 12、source 由 `dnf` 改 `local`；`_dnf_empty` source 改 `missing` |
| `scripts/player/player_controller.gd` | 刪死常量 `DNF_NORM_ROOT`/`DNF_GAME_SCALE`；`DNF_WHO` → `PLAYER_ART_ID="player"` |
| `scripts/ui/ui_skin.gd` | 刪 `DNF_UI_ROOT`、`TEX_DNF`（12 鍵）、`dnf_texture()`；封面回退改生態 backdrop |
| `scripts/ui/main_menu_screen.gd` | 刪英雄立繪/武器展示欄常量與 `_build_showcase()`、`_hero_atlas()`、`_add_hero_shadow()`；封面優先序改 main_menu_bg → forest → 純黑 |
| `scripts/core/content_paths.gd` | 註釋舉例改 `PACK_CREATURE_ROOT` |
| `tools/capture_main_menu.gd` | 僅更新用途註釋 |
| `tools/verify_anim.gd` | B 段改 16 怪 `pack_load_set()`（4/4/4/3/6 × 四方向）；C 段玩家改 pack 八方向；D/E/G 段移除 dnf/use_manifest 哨兵 |
| `tools/verify_ui_assets.gd` | 封面刪 `dnf_texture("cover")`；玩家由 A/B 兩套改單條 `pack_load_set("player")` |

**保留的歷史命名**（名字帶 dnf 但只解析豆包/用戶目錄，是通用幀解析器，無第三方含義，勿刪）：
`dnf_load_dir`、`dnf_load_user_dir`、`dnf_parse_names`、`dnf_load_png_texture`、`_dnf_build_clips`、`_dnf_actions_of`、`_dnf_first_texture`、`_dnf_empty`、`_dnf_sorted`、`_dnf_cache`，常量 `DNF_GAME_SCALE=0.25`、`DNF_FEET_Y=12.0`。

---

## 四、清理後角色解析鏈

`enemy_base.gd::resolve_character_set(id, bundled_path="")`：

1. `user://content/characters`（用戶目錄）
2. **`res://assets/pack/creatures`（豆包，committed：16 怪 ×84 = 1344 幀 + 玩家 71 幀）**
3. bundled 單張（`assets/sprites/enemies`，16 檔豆包像素）
4. **空集回退**（原 dnf/normalized 已刪除）

---

## 五、驗證證據（2026-09-22）

- `godot --headless --import --path game`：**exit 0**，全局類註冊正常。
- `python tools/run_regression.py`：**54 個腳本全綠 + self_check「全部通過（111 項）」，77.2s，FAIL=0**（verify_anim、verify_ui_assets 均 FAIL=0）。
- 危險引用掃描（`assets/dnf`、`dnf_texture`、`TEX_DNF`、`DNF_UI_ROOT`、`dnf_load_set`、`dnf_read_manifest`、`DNF_NORM_ROOT`、`aigei`、`hero_standee`、`verify_dnf`）在 `game/` 的 `.gd/.tscn/.json/.tres/.cfg` **歸零**。
- 全專案圖片檔名第三方關鍵詞掃描（dnf/kof/street_fighter/marvel/capcom/pvz/minecraft/sanguo/shanhaijing/rpg_maker/album 等）**歸零**。
- 遊戲內圖片 1827 張全部為豆包素材：pack/creatures 1415、ui/quest 32、icons/equipment 18、ui/pixel 17、sprites/enemies 16、characters 三職業包、fx、anim、weapons、armor、monsters、backgrounds、tilesets、previews；`assets/` 之外無散落圖片。

---

## 六、刪除產生的待補清單（後續補，本輪只記錄）

| # | 項目 | 現狀 | 補的時候要做的事 |
|---|---|---|---|
| 1 | 主選單英雄立繪 | 原 `hero_standee`（第三方，180×236，底裁 14px）已移除，封面走背景圖 | 出豆包像素英雄立繪，重新接回 `main_menu_screen` |
| 2 | 主選單武器展示欄 2 圖 | 原 `weapon_sword_blue/green`（展示 96×96、源 48×48）已移除，`_build_showcase` 已刪 | 出豆包像素武器圖後，**需重新接線**（展示欄代碼已整體移除） |
| 3 | 玩家 idle/hurt/death 方向 | walk/attack 八方向齊（64 幀）；idle/hurt/death 僅朝南（idle_s 1、hurt_s 2、death_s 4），其餘方向走 fallback | 補 idle/hurt/death 七方向（或鏡像） |
| 4 | 16 怪動畫精度 | 程序化形變微動（非逐幀手繪肢體），四方向 | 要更精細需逐幀手繪或骨骼輪（見 PACK_OVERVIEW §11.7） |

**資料/接線缺口（非素材、非版權問題，本輪不動）**：hammer/orb/quiver/offblade 有圖無裝備表資料條目；set_emblem 3 圖無 UI 消費；`assets/characters/`、`assets/monsters/`、`assets/ui/pixel/`、`assets/anim/`、`assets/fx/` 有圖無人接（默認保留）。

---

## 七、保留項（已核實，非第三方或非圖片）

- **字體** `game/assets/fonts/`（ChillBitmap_16px、Cubic_11）：SIL OFL 可商用開源像素字體，非圖片、文字渲染必需，**保留**。
- **豆包素材包**：`game/assets/` 全部、`deliverables/pixel_pack_2026-09-21/`、`deliverables/tileset-v2-2026-09-22/`、`deliverables/seven-legends-ui-prototype/`、項目根 `ui/`（330 張，豆包像素包早期副本）、`.fxwork/`（豆包 fx 程序化工作目錄）。
- **工具運行時/開源庫**：隔離區 `dot_vu/pylibs`（Pillow，MIT）、`dot_vu/font_dl`（OFL 字體）、`dot_vu/Godot`（引擎快取/saves/日誌），均非第三方美術素材。

---

## 八、可進一步瘦身（可選，不影響遊戲）

- `deliverables/_quarantine_2026-09-22/` 殘留 1240 檔 / 47.8 MB（其中圖片 638 張，均為豆包副本：asset-swap 274、pixel_pack 361、tilesets 3；dot_vu 圖片已清零），是**豆包素材副本 + 開發環境快照**（`asset-swap-2026-09-22`、`pixel_pack_2026-09-21` 副本、`tilesets_procedural`、`dot_vu` 的 PIL/OFL 字體/Godot 快取/saves/日誌），遊戲不引用、正本在 `game/assets` 與 `deliverables/pixel_pack_2026-09-21`，**可整個刪除**。
  - 唯一需先取出的是 `dot_vu/Godot/app_userdata/七傳說/saves/slot_03.json`（開發期存檔，若要留）。
- `deliverables/gstack/` 已清空，僅留空目錄，可刪目錄。
- `.workbuddy-ai/` 為另一工具的工作目錄：已刪其中第三方抓取快取 `aigei-profile`；其 `backups`（程式碼備份）、`memory`、`tmp`、`trash` 非美術素材，未動。

---

## 九、全專案圖片分布（清理後審計）

| 位置 | 圖片數 | 性質 |
|---|---:|---|
| `game/` | 1827 | 遊戲本體，全部豆包素材 |
| `deliverables/pixel_pack_2026-09-21/` | 1351 | 豆包像素包正本 |
| `deliverables/_quarantine_2026-09-22/` | 638 | 豆包副本（第三方圖已清，可整刪） |
| `deliverables/seven-legends-ui-prototype/` | 92 | 豆包 UI 原型 |
| `ui/`（項目根） | 330 | 豆包像素包早期副本 |
| `deliverables/tileset-v2-2026-09-22/` | 3 | 豆包地磚 |
| `.fxwork/` | 18 | 豆包 fx 工作目錄 |
| `.workbuddy-ai/` | 0 | 第三方圖已清零 |

> 索引同步：`game/assets/PACK_OVERVIEW.md` 已新增「十二、非豆包素材版權清理（2026-09-22）」，並訂正第十一章關於「第③級 normalized 保留」的過時描述。
