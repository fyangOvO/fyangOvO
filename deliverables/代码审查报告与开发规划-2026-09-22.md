# 七傳說 · 代碼審查報告 ＋ 遊戲開發規劃（頁面驅動）

> 日期：2026-09-22 ｜ 引擎：Godot 4.7.2 stable（專案標註 4.3 特性，4.7.2 實跑兼容）
> 範圍：`D:\七傳說\game` 全部腳本 / 場景 / 數據 / 素材
> 進度：步驟 0–6 已完成（首頁 → 角色選擇 → 技能管理面板 → 裝備系統頁 → 據點六面板統一 → 局內 HUD＋戰鬥體驗），下一步 = 步驟 7 系統收尾

---

## 一、代碼審查結論（先解決報錯）

### 1.1 實測驗證結果（本機重跑，非紙面）

| 檢查項 | 方式 | 結果 |
|---|---|---|
| 全項目腳本語法 | `parse_all` 掃 196 個 .gd | ✅ 全部可解析 |
| 完整自檢 | `self_check` 112 項 | ✅ 0 失敗（exit=0） |
| 敵人生態（歷史失敗） | `verify_enemy` | ✅ 0 失敗（修復已生效） |
| 命中判定（歷史失敗） | `verify_hit` | ✅ 0 失敗（修復已生效） |
| 擊退系統（歷史失敗） | `verify_knockback` | ✅ 0 失敗 |
| 關卡生成（歷史失敗） | `verify_level_gen` | ✅ 0 失敗 |
| 技能系統 | `verify_skills`（12 技能 + 法力/冷卻/效果） | ✅ 0 失敗 |
| 角色選擇 | `verify_class_select`（數據/佈局/切換/建檔流） | ✅ 0 失敗 |
| 技能面板 | `verify_skill_panel`（數據/存檔/面板/交互/控制器） | ✅ 0 失敗 |
| 端到端 | `verify_e2e`（主菜單→選職→據點→關卡→結算） | ✅ 0 失敗 |
| 存檔 | `verify_save81` | ✅ 0 失敗 |
| UI 素材 | `verify_ui_assets` | ✅ 0 失敗 |
| 首頁真渲染 | `capture_main_menu` 截圖 | ✅ 渲染正常 |

**歷史報錯回顧**：2026-09-21 回歸曾報 `verify_enemy` 3 項失敗與 `verify_hit` 1 項失敗，當天已修復；本次複跑全部通過，**無需再修**。步驟 3 新增 4 個職業專屬技能後，`verify_skills` / `verify_class_select` / `self_check` 的舊技能數量錨點（8 技能 / 每職 4 技能）已同步更新為 12 技能 / 職業池 3–5。

### 1.2 剩餘問題清單（非致命，列入待辦）

| # | 位置 | 問題 | 影響 | 處置建議 |
|---|---|---|---|---|
| 1 | 整個工作區 | 大量未提交改動（素材版權清理刪除 DNF 素材 + 新增豆包像素包 + 各步驟改動），未 commit | 無功能影響，但無提交點可回滾 | 確定素材歸位後分批提交 |
| 2 | `game/tools/capture_main_menu.gd:15` | 輸出路徑寫死到不存在的目錄 `deliverables/quest_ui_equipment_pack_2026-09-21/` | 截圖工具直接報錯 | 已建目錄解決；建議改為可配置路徑 |
| 3 | 無頭運行退出 | RID/ObjectDB 泄漏警告 | 無頭測試常見、非遊戲內問題 | 可暫不處理 |
| 4 | `level_scene.gd` 多處 | 目標類型 `survive`/`reach_exit` 未實現，退化為清空全圖並 push_warning | 部分章節目標與設計不符 | 列入關卡玩法深化工單 |
| 5 | `loot_roller.gd:84` | 消耗品未實現，掉落回退為金幣 | 缺少藥水等消耗品 | 列入局內系統工單 |
| 6 | `main_menu_screen.gd:24` | 第三方立繪 / 武器展示因版權清理移除 | 首頁視覺 | 步驟 1 已用自產三職業立繪補上 |
| 7 | 弓箭系技能圖標 | 新技能（穿透箭/箭雨/蓄力斬）暫用物理斬圖標佔位 | 純視覺 | 見步驟 3 素材清單，等你拍板 |

**結論：報錯問題已清零，代碼基線健康。**

---

## 二、開發規劃（頁面驅動 · 一點點來）

原則：**不做全局開發，按「頁面 → 系統」逐層推進**。每步都有可運行、可截圖、可驗證的產出。demo 一律按 640×360 小窗呈現，正式版可全屏（整數倍拉伸 + Nearest）。

### 步驟 0：代碼基線確認 ✅（2026-09-22）
- 報錯清零、自檢全綠、首頁可渲染。

### 步驟 1：首頁（主菜單）定稿 ✅（2026-09-22 已完成）
- **素材**：標題 LOGO 升級（金色龍紋徽章底板 + 燙金像素「七傳說」+ 副標題烘焙）；壁掛火把 ×2（火焰 flicker + 火星粒子）；三職業立繪（戰/弓/法，精緻像素插畫，透明底）。
- **代碼**：`ui_skin.gd`（+6 素材邏輯名）；`main_menu_panel.gd`（標題區改徽章底板 336×112，垂直節奏 8+112+8+224+8=360）；`main_menu_screen.gd`（戰士立繪/火把/粒子/版本欄/標題浮動）；`verify_ui_assets.gd`（斷言同步）。
- **驗證**：解析全綠 ✅；UI 素材驗證 0 失敗 ✅；完整自檢 0 失敗 ✅；真渲染截圖 ✅。
- **截圖**：`deliverables/首页定稿_主菜单_2026-09-22.png`。

### 步驟 2：角色選擇 / 角色設定頁 ✅（2026-09-22 已完成）
- **拍板方向**：首頁「開始遊戲」後彈角色選擇頁；經典分工 戰士(近戰高血)/弓箭手(遠程高暴擊)/法師(法術高爆發)；佈局 = 職業描述 + 數值預覽 + 立繪大圖 + 技能展示。
- **數據**：`data/classes/classes.json`（3 職業：display_name / title / playstyle / description / stats 五維 / skills 職業池 / default_skill_bar 默認出戰欄 / portrait / color）。
- **職業數值（Lv.1 基準）**：戰士 95HP/15攻/12甲/3.4速/5%暴；弓 70/13/6/4.2/20%；法 60/19/4/3.6/8%。
- **代碼**：`character_select_panel.gd`（四欄佈局：職業按鈕 / 188×250 立繪 / 職業名·頭銜·定位·描述·2×5 數值·技能卡 / 返回·確認）；`game_constants.gd`（SKILL_ICON 上移共享、CLASS_DEFAULT、SAVE_VERSION）；`save_data.gd`（class_id + 遷移）；`save_manager.gd`（create_new_slot 帶職業）；`main_menu_screen.gd`（開始遊戲→選職→建檔進據點）；`hub.gd`/`stat_panel.gd`（據點屬性面板標題顯示職業名）。
- **驗證**：`verify_class_select` A/B/C/D 全綠 ✅；`verify_e2e` 全綠 ✅；`verify_ui_assets` 0 失敗 ✅；`self_check` 全綠 ✅；真渲染截圖 ×3 ✅（技能卡動態 4–5 張，隨職業池）。
- **素材**：三職業立繪沿用步驟 1，**無新素材需求**。
- **截圖**：`deliverables/角色选择_战士|弓箭手|法师_2026-09-22.png`。

### 步驟 3：技能系統頁（據點技能管理面板）✅（2026-09-22 已完成）
- **拍板方向（三問三答）**：① 技能頁放**據點內技能管理面板**（查看 / 重排技能欄；升級系統放後面，本頁不寫）；② **職業專屬技能池**（戰士=近戰技、法師=元素技、弓箭手=遠程技）；③ 升級/形態變化留後續。
- **數據**：`skills.json` 8→12（新增 蓄力斬 / 穿透箭 / 箭雨 / 淬毒箭，slot=0 備選池）；`classes.json` 技能池（戰 5 / 弓 4 / 法 5）+ `default_skill_bar`；ConfigLoader 存 `default_skill_bar` + 校驗（非空 + 均在池內）+ `class_default_skill_bar()` 訪問器。
- **存檔**：`save_data.gd` 新增 `skill_bar`（序列化 + 落盤）；`SAVE_VERSION` 2→3；v1/v2 舊檔遷移自動補職業默認欄（while 步進，v1 可一路升到 v3）。
- **出戰欄取欄鏈**（`skill_controller.gd`）：存檔 `skill_bar`（非空）→ 職業默認欄 → 戰士默認欄（兼容無檔測試/舊行為）。重排後即時生效於局內 1/2/3 鍵施放。
- **UI**：`skill_panel.gd`（新面板：技能池 5 卡 + 出戰欄 3 槽[鍵位角標] + 恢復默認/保存；交互=點池卡裝配/點槽卸下/欄滿拒絕提示）；`hub.gd` 註冊 `PANEL_SKILLS`（按鈕條第 6 項）+ `_on_skill_bar_saved` 落盤。
- **修復（步驟 3 順手）**：據點浮層 `box` 的 PRESET_CENTER 錨點在零尺寸時設置，容器變大後沿右下增長 → 面板被視口裁切（**所有據點面板的既有 bug**）。改 `grow_horizontal/vertical = BOTH`，面板從中心向四周展開。
- **驗證**：`verify_skill_panel`（新工具：A 職業池/默認欄 → B 存檔 v3+遷移 → C 據點面板結構 → D 交互[裝配/卸下/重排/保存/恢復默認] → E 控制器讀檔聯動）全綠 ✅；`verify_skills` / `verify_class_select` / `verify_e2e` / `verify_save81` / `verify_ui_assets` / `self_check`(112) 全部複跑全綠 ✅。
- **截圖**：`deliverables/技能面板_默认出战栏_2026-09-22.png` / `技能面板_重排后_2026-09-22.png`（小窗 640×360）。

### 步驟 4：裝備系統頁 ✅（2026-09-22 已完成）
- **拍板方向**：① 裝備面板定稿——據點「裝備欄」重做視覺：六格裝備位（頭/衣/手/腿/鞋/武器）+ 已裝備清單 + 詞綴展示，與技能面板同一套視覺規範，按稀有度 8 檔等級色（特效渲染後續再做，先簡單版）；② 背包整理——格子瀏覽 / 稀有度排序 / 丟棄，查看級 + 拖拽；③ 裝備對比——點裝備彈浮窗，與當前穿戴橫向對比數值。
- **UI 改版**：
  - `equip_panel.gd`（重寫）：六格裝備位（3×2，槽位邊框按稀有度著色、裝上顯示圖標）+ 右側已裝備清單（RichTextLabel 稀有度色）+ 詞綴展示（點選槽位 → 基礎屬性 + 詞綴列表）+ [卸下] 回調；副手/項鏈/戒A/戒B 暫不入面板（數據與結算保留）。
  - `inventory_panel.gd`（重寫）：背包/倉庫 8 列格（稀有度邊框 + 圖標）+ 快捷裝備條（六格，拖拽目標，點擊已裝備卸下）+ 工具欄 [按稀有度][丟棄][倉庫] + 點格子彈對比浮窗；格拖拽 payload = {type:"bag_item", instance_id, slot}，拖到對應槽位裝備（武器只能落武器槽）。
  - `equip_compare_popup.gd`（新）：置頂浮窗 = 複用 `ComparePanel` 對比行（綠升紅降灰平 + 差異合計）+ 副標題新舊裝備名（稀有度色）+ [裝備此件][關閉]；槽位為空顯示「可直接裝備」。
- **hub 接線**：`_do_equip(index)` 抽核心穿裝邏輯；`_on_equip_instance(instance_id)`（拖拽/浮窗用）；`_on_drop_item(instance_id)`（背包/倉庫通用移除 + 落盤）；`_on_compare_requested(item)` + 懶建置頂浮層。
- **修復**：對比浮窗 holder 初始 hidden、只 show popup 導致遮罩不顯示的 bug（show 時開 holder、close 回調隱藏）。
- **驗證**：`verify_equip_panel`（新工具 A–E：面板結構/背包交互/拖拽契約/對比浮窗/hub 接線）34 項全綠 ✅；`verify_hub`（10 槽→六格斷言同步）✅；`verify_equipment_compare` / `verify_skills` / `verify_class_select` / `verify_skill_panel` / `verify_ui_assets` / `verify_save81` / `verify_e2e` / `self_check` / `parse_all` 全部複跑全綠 ✅。
- **素材**：全部沿用既有（裝備圖標 62 件 / 稀有度格子框 8 階 / 槽位底），**無新素材需求**。
- **截圖**：`deliverables/装备栏_六格装备位_2026-09-22.png` / `背包_快捷装备条_2026-09-22.png` / `装备对比_浮窗_2026-09-22.png`（小窗 640×360）。

### 步驟 5：據點（Hub）整體 UI 定稿 ✅（2026-09-22 已完成）
- **拍板方向**：六面板統一視覺（角色/天賦/鍛造仍是舊樣式，統一成背包/裝備/技能的新視覺規範）。
- **統一規範（三面板一致）**：`custom_minimum_size=(400,0)` / margin 14 / 標題 16px 職業色（ConfigLoader.classes color：戰士 D9A441 / 弓 8FBF5F / 法 7FA8E8）/ 提示行 12px / 內容 12px / 行按鈕 UISkin 皮膚（gold/dark，56×26）。
- **代碼**：`stat_panel.gd`（show_stats 增 class_id 參數；**31 行 × 2 列改 3 列**——原 2 列 ≈ 31 行高 525px，640×360 視口裁到「暴擊率」，改每行 3 項 HBox 後 11 行全屏可見）；`talent_panel.gd`（標題「天賦樹·職業名」+ 提示行「天賦點為賬號共享 分支按賬號等級解鎖（戰爭L1/秘法L15/影行L30）」+ 分支卡 200→116 寬、描邊 2→1px + `_title` 成員）；`forge_panel.gd`（標題「鍛造台·職業名」+ 提示行 + 行內按鈕改 `_make_row_btn` UISkin + `_title` 成員）；`hub.gd`（talent/forge bind 傳 class_id）。
- **修復（順手抓的真 bug）**：① 天賦「未解鎖」文案原用 `branch_cost()`（=20）當等級，所有分支顯示 Lv20，且用 TalentTree 的 might/guardian/arcane 鍵查面板鍵（war/mage/shadow）→ Lv99；改面板自帶 `BRANCH_UNLOCK`（war=1/mage=15/shadow=30）後：戰爭=解鎖、秘法=Lv.15、影行=Lv.30 正確。② 角色屬性面板超高裁切（見上）。
- **驗證**：`verify_panel_unify`（新工具 A 角色 / B 天賦 / C 鍛造 / D 統一規範）全綠 ✅；`verify_hub` / `verify_ui71` / `verify_ui_assets` 複跑全綠 ✅。
- **素材**：全部沿用既有，**無新素材需求**。
- **截圖**：`deliverables/角色属性_统一视觉_2026-09-22.png` / `天赋树_统一视觉_2026-09-22.png` / `锻造台_统一视觉_2026-09-22.png`（小窗 640×360）。

### 步驟 6：關卡內 HUD ＋ 戰鬥體驗 ✅（2026-09-22 已完成）
- **拍板方向**：血藍條、局內技能欄、掉落提示視覺統一。
- **血藍條**：`HealthBar` 增 `value_kind`（hp/mana）——mana 模式讀 `target.get_mana_pool()`、無護盾層、藍漸變（3A5FB0→1F3468，fallback 頂線 6E9BE8）；`level.tscn` HpBar 縮窄 `(24,128,172,16)` + 新增 MpBar `(204,128,96,16)`（bar_mp 貼圖）。法力池（ManaPool）首次有 UI。
- **局內技能欄**：`SkillBarUI`（新，`scripts/ui/skill_bar.gd`）3 槽（1/2/3 鍵）48px 右對齊原點 (472,296) 防 640 溢出；每槽 = 技能圖標（GameConstants.SKILL_ICON→UISkin）+ 鍵位角標（右下 1/2/3 暗底+1px 黑邊）+ 冷卻遮罩（槽頂向下按 剩餘/總冷卻 比例變暗 + 剩餘秒數）；`level_scene.gd` 靜態 4 槽改掛 SkillBarUI（SKILL_BAR_SLOTS 刪除）。
- **掉落提示**：`PickupToastHUD`（新）右上 (444,64) 逐條彈「金幣 +N（金）/ 魔石 +N（藍）/ 已拾取：裝備名（稀有度色）」，2.2s 後 0.35s 淡出、上限 5 條；`EventBus.loot_picked_up` 簽名改 Dictionary（原擴展零消費者），`LootDrop._pick_up()` 廣播；`level_scene.gd` 掛載消費（README 審計「零消費者信號」清零）。
- **驗證**：`verify_hud`（新工具 A 法力條 / B MpBar 節點 / C SkillBarUI 3 槽+冷卻比例 0.625 / D 提示內容+上限 / E 關卡接線）全綠 ✅；`verify_panel_unify` / `verify_ui_assets`（HpBar 新 rect + MpBar 斷言）複跑全綠 ✅；全量回歸（hub/ui71/ui_assets/equip/skill_panel/class_select/skills/save81/e2e/self_check/parse_all）全綠 ✅。
- **素材**：全部沿用既有（bar_hp/bar_mp 貼圖、技能圖標、面板 9-slice），**無新素材需求**。
- **截圖**：`deliverables/局内HUD_血蓝条技能栏_2026-09-22.png`（血藍並排 + 3 槽技能欄鍵位角標）/ `局内HUD_冷却与拾取提示_2026-09-22.png`（施法後冷卻遮罩 + 剩餘 5.5s + 已拾取烈焰劍/金幣 +12 提示）（小窗 640×360）。

### 步驟 7：系統收尾
- 音效 / 存檔 / 設置 / 導出驗證 / 素材版權清理收尾與分批提交。

### 步驟 7：系統收尾
- 音效 / 存檔 / 設置 / 導出驗證 / 素材版權清理收尾與分批提交。

> 每一步結束都有：可運行版本 ＋ 真渲染截圖（小窗 demo）＋ 回歸驗證全綠。

---

## 三、UI 設定規範（現有代碼，可直接沿用）

| 項目 | 設定值 | 出處 |
|---|---|---|
| 視口 | 640×360，整數倍拉伸，Nearest 過濾；demo 小窗、正式版全屏 | project.godot |
| 按鈕 | 金/暗三態 `btn_*_128x24` 2× ⇒ 256×48 | ui_skin.gd |
| 字號白名單 | {11, 12, 16, 22, 32}（像素鐵律）；技能卡/槽內文字 12px | GameConstants |
| 據點面板 | 浮層 = 全屏壓暗(0,0,0,0.6) + PanelContainer 居中（grow BOTH，步驟 3 修復） | hub.gd |
| 技能圖標 | 48×48 原生 1×；角色選擇卡內 24px（0.5× 整數） | skill_panel.gd |
| 職業色 | 戰士 D9A441 / 弓 8FBF5F / 法 7FA8E8 | classes.json |
| 據點面板統一（步驟 5） | 六面板同規範：min 寬 400 / margin 14 / 標題 16px 職業色 / 提示 12px / 內容 12px / UISkin 按鈕 | stat/talent/forge_panel.gd |
| 血藍條（步驟 6） | HpBar (24,128,172,16) 紅漸變 C42B2B→8C1A1F；MpBar (204,128,96,16) 藍漸變 3A5FB0→1F3468 | level.tscn / health_bar.gd |
| 局內技能欄（步驟 6） | SkillBarUI 3 槽 48px 1×、間距 4、原點 (472,296)、鍵位 1/2/3 角標 + 冷卻遮罩/秒數 | skill_bar.gd |
| 掉落提示（步驟 6） | PickupToastHUD 右上 (444,64) 寬 184、2.2s+0.35s 淡出、上限 5 條 | pickup_toast.gd |

---

## 四、素材清單（等你決定）

### A. 已有素材（無需補）
- ✅ 背景 / 標題橫幅 / 面板 / 按鈕三態 / 徽記 / 音效
- ✅ 三職業立繪（188×250 透明底）、標題徽章、火把
- ✅ 技能圖標 6 枚（slash / fireburst / frostnova / shadowdash / lightning_chain / poison_cloud）+ skill_slot 底槽

### B. 步驟 3 新增素材（弓箭系圖標，2026-09-22 已拍板並產出）

| # | 素材 | 用途 | 現狀 |
|---|---|---|---|
| 1 | **弓箭系技能圖標 ×2**（穿透箭 / 箭雨，48×48 像素風，與現 6 枚同套） | 弓箭手職業池區分度 | ✅ 已生成並接入（`skill_icon_piercing_shot_48.png` / `skill_icon_arrow_rain_48.png`） |
| 2 | 蓄力斬圖標 ×1 | 戰士職業池 | ⚠️ 按拍板暫用斬擊圖標佔位（`skill_icon_slash`） |
| 3 | 淬毒箭圖標 | 弓箭手 | ⚠️ 按拍板暫用毒雲圖標（`skill_icon_poison_cloud`） |
| 4 | 技能管理面板背景 / 卡片框（可選） | 面板視覺 | ⚠️ 目前用 StyleBoxFlat 純色框，可用 |

> 已生成 2 枚弓箭系圖標（用現 6 枚同套圖標做風格錨點延展，深底 (20,23,28) + 居中單主體 + 元素色高光，NEAREST 縮至 48×48），接入後弓箭手技能池/角色選擇卡均顯示新圖標；蓄力斬、淬毒箭維持佔位。

### C. 後續步驟素材預告
- 步驟 4 裝備頁：裝備圖標全套已有、格子框 8 階已有；主要缺面板視覺定稿（可代碼完成，無需新圖）。
- 步驟 5–6：據點/HUD 視覺統一，全部沿用既有素材，**無新素材需求** ✅。
- 步驟 7+ 預告（局內玩法深化時再列）：藥水/消耗品圖標、BOSS 立繪、地形素材、音效包。

---

## 五、決定記錄

| 日期 | 決定 |
|---|---|
| 2026-09-22 | 步驟 1：LOGO 升級 + 動態氛圍（火把/粒子）+ 三職業立繪全做，風格=更精緻像素插畫 ✅ 完成 |
| 2026-09-22 | 步驟 2：開始遊戲→彈角色選擇頁；經典職業分工；四欄佈局 ✅ 完成 |
| 2026-09-22 | 步驟 3：技能頁放據點內技能管理面板；職業專屬技能池（戰近戰/法元素/弓遠程）；升級系統放後面、本頁不寫 ✅ 完成 |
| 2026-09-22 | 步驟 3 素材：弓箭系圖標 ×2（穿透箭/箭雨）生成並接入；蓄力斬暫用斬擊、淬毒箭暫用毒雲 ✅ 完成 |
| 2026-09-22 | 步驟 4：裝備面板六格定稿（頭/衣/手/腿/鞋/武器 + 清單 + 詞綴 + 稀有度色）+ 背包瀏覽/排序/丟棄/拖拽 + 點裝備對比浮窗 ✅ 完成 |
| 2026-09-22 | 步驟 5：據點六面板統一視覺（角色/天賦/鍛造 → 背包/裝備/技能同規範，min400/margin14/標題16職業色/提示行/UISkin 按鈕）✅ 完成 |
| 2026-09-22 | 步驟 6：局內 HUD＋戰鬥體驗（血藍條並排、SkillBarUI 3 槽鍵位角標+冷卻遮罩、PickupToastHUD 掉落提示，loot_picked_up 零消費者信號補消費）✅ 完成 |
