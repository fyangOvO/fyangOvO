# 2026-09-17

## 阶段 6.2 怪物扩充 / 精英词缀怪（已提交）

- **数据**：`data/monsters/monsters.json` 8→16（新增孢蘑菇 / 暗影猎犬 / 烬石魔像 / 炼狱小鬼 /
  灰烬猎犬 / 焰术信徒（精英）/ 冰封尸骸 / 寒霜幽魂（精英），三章等级带全覆盖）。
- **词缀**：`scripts/enemies/affix_controller.gd`（class_name AffixController 纯静态）——
  6 词缀（急速 ×1.35 / 吸血 20% / 爆炸 40px·80% / 回响 30% / 荆棘 15% / 闪现 6s），
  权重随机 1–2 条不重复。
- **敌人集成**：`scripts/enemies/enemy_base.gd` +affixes/_move_mult/_phase_timer 与 6 处钩子
  （_ready / 巡逻追击速度 / _tick_state 闪现 / _attack_player 吸血回响 / take_damage 荆棘 /
  _on_unit_died 爆炸）。
- **验证**：verify_monsters62 24 项 7 段 0 失败；main.gd 自检 80→83（+怪物 16 种 + 精英 +
  词缀池）；回归 26→27 脚本全绿；渲染预览 build/monsters_preview.png 验收通过。
- **README** 5.31 节 + 完成表 6.2 行 + 实测表（83/83、24/24）+ 数据表怪物 8→16。
- **提交**：`6.2 怪物扩充：16 种 + 精英词缀怪（急速/吸血/爆炸/回响/荆棘/闪现）`。
- **踩坑沉淀**：verify 脚本自身 _ready 里直接 root.add_child 会失败
  （"Parent node is busy setting up children"）→ 改 `add_child.call_deferred` + 双 process_frame；
  DamageDummy 无 get_current_hp（用 hp 字段）；ConfigLoader 无 get_all_monster_ids（用
  monsters.keys()）；static 函数调用脚本类方法名 get_name 与内置冲突（直接读 AFFIXES dict）。

## 阶段 6.3 BOSS 设计与机制（已提交）

- **数据**：`data/bosses/bosses.json`（独立目录 `data/bosses/`，避免与怪物表同目录误解析）——
  2 个章末 BOSS：骸骨暴君（骨系：基础近战→召唤骷髅/暗影猎犬→范围践踏→狂暴）、
  熔心之主（火系：火球→召唤小鬼/灰烬猎犬→熔岩喷发→狂暴）；阶段门 75/50/25%，
  阶段伤害乘区递增，狂暴攻速 ×0.55–0.6 / 伤害 ×1.3–1.35。
- **控制器**：`scripts/enemies/boss_phase_controller.gd`（class_name 纯静态）——
  current_phase / phase_skills（累积去重）/ summon_count_for / enrage_multipliers / validate。
- **集成**：enemy_base.gd BOSS 钩子（_ready 加载阶段 1 / take_damage 后阈值切换 +
  广播 boss_phase_changed / _attack_player 施放召唤+AoE / 攻速与伤害乘狂暴）；
  event_bus.gd +boss_phase_changed(enemy, phase, skills)；config_loader.gd +bosses 注册表
  +get_boss(id) +交叉校验（BOSS 在怪物表 / 召唤池怪物存在）。
- **验证**：verify_boss63 32 项 7 段 0 失败；main.gd 自检 83→86；回归 28 脚本全绿；
  渲染预览 build/boss_preview.png 验收通过。
- **README** 5.32 节 + 完成表 6.3 行 + 实测表（86/86、32/32）+ 数据表 BOSS 机制 2。
- **提交**：`6.3 BOSS 机制：骸骨暴君/熔心之主 4 阶段（召唤/范围/狂暴）`。
- **踩坑沉淀**：LootTable 用 drop_count_range（Vector2i）非 drop_count_min/max；
  新数据目录要与已有目录区分（同目录扫描会全量误解析）；
  静态类 skill 名 get_name 等与内置冲突（用 dict 直读）。

## 阶段 6.4 技能库扩充（已提交）

- **数据**：`data/skills/skills.json` 3→8——出战（slot 1-3）裂斩/旋刃/突进不变；
  备选（slot 0）冰霜新星（AoE 冰 110% r64）/ 火球术（单体火 240% r140）/ 闪电链（单体雷 180% r130）/
  毒云（AoE 毒 85% r56）/ 暗影步（位移影 120% d120），覆盖三形态 + 六元素。
- **槽位语义**：SkillData.validate slot 放宽 0-3（0=备选）；SkillController._load_skills
  只加载 slot ≥1（技能栏 1/2/3 不被备选占用）；备选供局内成长替换接入。
- **元素表**：game_constants.gd +ELEMENT_SHADOW → ELEMENTS 5→6 系；
  verify_damage「元素系 5」断言同步改 6。
- **验证**：verify_skills 45→49 项 0 失败；main.gd 自检 86→88；回归 28 脚本全绿；
  渲染预览 build/skills_preview.png 验收通过。
- **README** 5.33 节 + 完成表 6.4 行 + 实测表（88/88、49/49）+ 数据表技能 3→8。
- **提交**：`6.4 技能库扩充：8 技能（3 出战 + 5 备选，6 元素）`。
- **踩坑沉淀**：SkillController 按 slot 排序加载会把 slot 0 排最前占栏 → 过滤 slot ≥1；
  ConfigLoader 技能 element 不在 ELEMENTS 时静默降级 physical（加 shadow 元素解决）；
  元素表扩容要联动 main.gd 自检「元素体系 5 系」与 verify_damage「元素系 5」断言。

## 阶段 6.5 装备库填充（已提交）

- **数据**：装备 42→62（weapons 8→14：+烈焰剑 22atk/刀、血斧 18atk+15hp、冰川锤 26atk+5穿、淬毒匕首
  14atk+8攻速、风暴法杖 20atk+5暴击、灵风长弓 12atk+12攻速；armor 8→14：+石像头盔 28hp+10甲、
  烬织胸甲 45hp+8甲、冰织手套 18hp+5甲、守望腿甲 52hp+6甲、疾风靴 15hp+10移速、
  泰坦王冠 60hp+12甲+4暴击 稀有起；jewelry 8→16：+余烬护符/霜戒/雷暴之戒/影纱吊坠/嗜血之戒/
  烈日圣印/回响之戒/守护者勋章），套装件 18 保持。
- **设计**：三章等级带覆盖（1-6/7-13/14-20 每章 ≥20 可用）、稀有度梯级（普通→传奇）、
  掉落权重普通 > 稀有。
- **踩坑**：弓类底材 archetype 合法值是 `longbow` 不是 `bow`（ConfigLoader 交叉校验 WARNING →
  在 weapons.json 与助手脚本都改 longbow）；槽位/稀有度枚举在 GameConstants（EquipSlot /
  WeaponArchetype / Rarity），不在 EquipmentData；预置神话件（mythic_staff_final_echo 主手 /
  mythic_crown_seven_kalpa 头盔）跨文件槽位导致部位分类计数对不上 → main.gd 自检断言放宽为覆盖性阈值。
- **验证**：verify_equipment65 14 项 0 失败；main.gd 自检 88→90；回归 28→29 脚本全绿；
  渲染预览 build/equip_preview.png（10 槽位分列 + 稀有度色条 + 新增 * 标注）验收通过。
- **README** 5.34 节 + 完成表 6.5 行 + 实测表（90/90、14/14）+ 数据表装备 42→62。
- **提交**：`6.5 装备库填充：42→62 件（三部位 + 稀有度/等级带覆盖）`。

## 阶段 6.6 音效/音乐（已提交）

- **方案**：程序化合成占位音效（与占位色块美术同思路，真音频可直接替换 wav）。
- **数据**：`data/audio/*.wav` 8 条（22050Hz 16-bit mono，`.vu/synthesize_audio_66.py` 用 Python
  wave 合成）：hit_melee 打击（方波 200Hz+噪声）/ hit_crit 暴击（340Hz+泛音）/ enemy_die 死亡
  （600→150Hz 扫频）/ pickup_gold 金币（1200→1800Hz 升调）/ pickup_item 装备（三角波）/ levelup
  升级（琶音 523/659/784Hz）/ boss_phase BOSS 阶段（锯齿 160→55Hz）/ ui_click 点击。
- **播放管线**：`scripts/audio/audio_manager.gd` 新 class_name AudioManager 纯静态——惰性预载 +
  临时 AudioStreamPlayer 播放即焚、零 autoload、无场景树静默（verify 无副作用）。
- **钩子**：enemy_base take_damage（被玩家命中 hit_melee，荆棘反弹二次不响）/ _on_unit_died
  （enemy_die）/ _apply_boss_phase（boss_phase）；player_controller pickup_loot（金币材料
  pickup_gold、装备 pickup_item）；run_progression add_xp 升级（levelup）。
- **验证**：verify_audio66 9 项 0 失败；main.gd 自检 90→92；回归 29→30 脚本全绿；
  渲染预览 build/audio_preview.png（8 卡片+波形折线）验收通过。
- **README** 5.35 节 + 完成表 6.6 行 + 实测表（92/92、9/9）+ 数据表音效 8。
- **提交**：`6.6 音效音乐：8 条合成占位音效 + 播放管线（真音频可替换）`。
- **坑**：新增 class_name（AudioManager）必须 --headless --editor --quit 刷类缓存，否则
  verify 报「Identifier not declared」；static 类方法在无场景树时调 get_tree() 会炸 →
  用 Engine.get_main_loop() as SceneTree 防御。

## 阶段 7 UI/UX（已提交，7.1–7.7 七项）

- **新增面板（纯代码构建 class_name，像素风 Theme）**：
  - 7.1 `main_menu_panel.gd` MainMenuPanel：标题 + 开始/设置/退出 + 账号概览（Lv/金币/魔石/声望加成），回调注入。
  - 7.2 `stat_panel.gd` StatPanel：属性总表（8 基础 + 4 抗性 + 6 元素伤害），数据源 StatCalculator。
  - 7.3 `equip_panel.gd` EquipPanel：10 槽装备栏（主/副手/头/胸/手/腿/靴/护符/戒A/戒B）+ 选中详情 + 穿/脱回调；背包网格复用 3.6。
  - 7.4 `talent_panel.gd` TalentPanel：三分支卡片 + 节点状态 + 点数 + learn 回调；数据源 TalentTree（node_id 格式 `branch.kind.index` 点分隔）。
  - 7.5 `forge_panel.gd` ForgePanel：费用/禁用/锻造/重铸 + 结果反馈；数据源 ForgeController（注意 cost 是 Dictionary）。
  - 7.7 `settings_panel.gd` SettingsPanel：画质（vsync/全屏 → DisplayServer）+ 音量（slider → AudioServer）+ 按键（10 动作 → InputRemapper）；持久化归 8.2。
- **增强**：7.6 `result_panel.gd` ResultPanel——奖励清单（前 6 件 + 超出计数）+ 返回大厅/再来一局按钮（on_hub/on_restart 回调）。
- **职责边界**：面板只读 + 回调转发，不写业务状态。
- **验证**：verify_ui71 24 项 0 失败；main.gd 自检 92→94；回归 30→31 脚本全绿；
  渲染预览 build/ui_preview.png（7 面板分区）验收通过；README 5.36 节 + 完成表 7.1-7.7 行。
- **提交**：`7.1-7.7 UI/UX：7 面板（主菜单/属性/装备/天赋/锻造/结算/设置）`。
- **踩坑沉淀**：GDScript lambda 捕获基本类型按值 → 验证标志用类成员变量；PALETTE_NEUTRAL 只有
  10 色（索引 0-9，[10] 越界）；EquipmentInstance 无参 new——用 create_from_template(tpl, lvl, rarity)
  工厂；ForgeController.get_forge_cost 返回 Dictionary（gold/stone/crystal/success_chance）；
  Button 文本不是 Label（计数断言要分函数）；TalentTree.node_id = "branch.kind.index"（点分隔）；
  GDScript 不支持行首 `+` 续行；main.gd 验证函数里变量名不能与既有 `m`/`e` 冲突。

## 阶段 8.1 完整存档（已提交）

- **现状盘点**：SaveData 已全字段序列化（局外 10+ 类 + 局内 current_level_id/current_difficulty_tier
  + settings + statistics），SaveManager 槽位/备份轮转/损坏隔离/原子写均已有；8.1 为「完整闭环实测」。
- **新增**：tools/verify_save81.gd|.tscn（15 项 6 段：新建槽位/局外全字段/局内进度/设置字典/
  装备实例回读/跨会话模拟/清理）全过；tools/save_preview.gd|.tscn → build/save_preview.png
  （局外/局内/设置 3 卡片）渲染验收通过。
- **踩坑**：SLOT=18 越界 → SAVE_MAX_SLOTS=8 用槽位 7；set_equipped(slot_index, item) 签名；
  内联 python -c 引号坏 → 一律先 Write .py 再跑（本次 append_regression 内联失败重写）。
- **收尾**：main.gd 自检 94→98（8.1 存档矩阵 + 局内进度、8.2 默认值 + 类型白名单）；
  main.gd 变量名 `sd` 与既有 sd 冲突 → 改名 sd81（GDScript 变量作用域是整个函数）；
  回归 31→33 脚本全绿；README 5.37/5.38 节（CRLF=1861 行）；run_regression.py 追加
  verify_save81/verify_settings82（注意列表项无路径前缀，统一 res://tools/%s.tscn）。

## 阶段 8.2 设置持久化（已提交）

- **实现**：scripts/core/settings_store.gd（class_name SettingsStore 纯静态）——
  user://settings.json，DEFAULTS {master_volume_db:-6.0/vsync:true/fullscreen:false/key_bindings:{}}，
  类型白名单 _typed 校验回退、非法 JSON 隔离（复制为 settings.json.corrupt_*）、
  _apply 联动 AudioServer/DisplayServer、load() 末尾调 InputRemapper.load_bindings() 回放按键。
- **接入**：SettingsPanel 画质/音量改读 SettingsStore 初始值 + 变更 set_value 即落盘。
- **踩坑**：FileAccess.open READ 后必须先 close 再写副本（Windows 句柄锁导致隔离文件写失败）；
  三目表达式语句 + rename_absolute 不生效 → 改「读→写副本→close」实现隔离；
  损坏隔离第一次 FAIL 是 FileAccess 句柄未释放，加 f.close() 后 12/12 全过。
- **验证**：verify_settings82 12 项 5 段（默认值/写入读回/跨加载保持+AudioServer 联动/类型回退+非法隔离/清理）。
- **注意**：settings.json 在 user:// 根（Godot appdata user data 目录），非项目目录；
  7.7 SettingsPanel 的 G 段音量断言不受影响（回归 verify_ui71 仍 24/24）。

## 阶段 8.3 性能优化（已提交）

- **实现**：
  - scripts/core/object_pool.gd（class_name ObjectPool 纯静态）：acquire(key, factory)/
    release(key, node)/register(key, capacity=64)；池内节点隐藏+移出活跃树+停 _process；
    池满 release 走 queue_free 兜底；hits/misses/reuse_rate/live/pool_size 统计；drain() 退出清理。
  - scripts/run/level_view.gd（class_name LevelView extends Node2D）：整个 layout（cells:
    Dictionary[Vector2i->int]）画进单个 CanvasItem._draw，逐格 draw_rect（地面/墙/障碍三色
    + 金色出生点）；tile_count()。
- **踩坑（重要）**：
  - Node 无 visible 属性（那是 CanvasItem）→ 池 release/acquire 用 `node is CanvasItem` 判断。
  - LevelGenerator.generate(level_def: LevelData, rng) 签名（不是 id）；cells 是 Dictionary
    （Vector2i key）不是 Array；player_spawn 是 Vector2i（int(Vector2i) 报 Nonexistent
    constructor → 用 key.x/key.y）。
  - 有 TILE_OBSTACLE=2 第三类格；cells.size() 与 width*height 可能不等（生成器 erase/改写），
    断言一律以 cells.size() 为基准。
  - GDScript `%` 格式化不支持 `%%` 转义（报 unsupported format character）→ 拼接字符串。
  - GDScript for 循环变量在循环外不可用（drain 里 _pools[key]=[] 移进循环内）。
  - headless 退出 ObjectDB 泄漏告警 → ObjectPool.drain() 在 verify 尾部调用。
- **验证**：verify_perf83 10 项 5 段（池基本盘/池满兜底/复用率≥90% 实测 96%/无泄漏/
  单节点承载 1184 格/地面墙障碍计数/批处理数组完整）0 失败无泄漏；
  main.gd 自检 98→100；回归 33→34 脚本全绿；
  perf_preview → build/perf_preview.png（左池统计 + 右批处理地图）验收通过；README 5.39。
- **接入就绪**：对象池接口已备，战斗层高频对象（敌人/掉落/飘字）后续接入；
  本项不改既有生命周期（verify_juice/loot 不受影响）。

## 阶段 8.4 平衡性调优 · 数值仿真（已提交）

- **仿真口径（D9 + 怪物数值 v1.5 已锁定）**：
  怪 HP=100.7×1.284^(L-1)×1.08^n / DMG=5.61×1.218^(L-1)×1.23^n（n=0..4，梦魇V≈伤×2.4/HP×1.36）；
  玩家 DPS=20×1.30^L×1.25 / MaxHP=500×1.06^L；TTK=怪HP/玩家DPS；承伤=MaxHP/(怪DMG×0.5/s)。
- **关键调优（真实平衡问题）**：初版玩家成长 1.10^L 压不过怪 HP 1.284^(L-1) →
  第 20 关 TTK 飙到 94s（海绵化）→ 玩家复合成长（等级+装备+技能）调至 1.30^L →
  TTK 收敛 2.4–4.2s。**「怪不海绵化、高层级允许被秒」的 D9 目标由此达成。**
- **仿真结论（8/8 全过）**：TTK∈[2.4,4.2]s 全区间；难度爬坡单调；梦魇III-V承伤<10s（实测5.9）；
  普通前5关>12s（实测108）；20关 TTK 2.4 ≤ 3.1×2。
- **产出**：tools/verify_balance84.gd|.tscn（8 项 5 段，输出 20×5 TTK/承伤矩阵）、
  tools/balance_preview.gd|.tscn → build/balance_preview.png（TTK 5 难度曲线 + 承伤曲线）验收通过；
  main.gd 自检 100→102（注意 main.gd 自检函数是 _add_check 不是 _ok）；
  回归 34→35 脚本全绿；README 5.40 平衡报告节。
- **踩坑**：main.gd 8.4 段误用 _ok() → Parse Error（函数名 _add_check）；
  Edit 时 old 串末尾换行错位把两行黏成一行 → 修复时整段重贴。

## 阶段 8.5 Bug 修复与回归测试（已提交 · 阶段 8 收尾）

- **回归**：全量 36/36 脚本全绿零告警零泄漏（新增 verify_fix85 后 35→36）。
- **verify_fix85.gd|.tscn**：8 项回归红线（存档槽位边界/字段矩阵/设置类型白名单/
  默认值镜像/对象池复用零泄漏/LevelView Dictionary+障碍格兼容/平衡 TTK 红线/
  数据表锚点 怪物16 BOSS2 技能8 装备62 关卡20）全过。
- **踩坑**：ConfigLoader 数据字段名是 equipment_templates 不是 equipment（SCRIPT ERROR
  但 0 失败——错误被吞，需主动扫 SCRIPT ERROR/WARNING）；verify 退出前 LevelView
  add_child 未 free → CanvasItem RID 泄漏（加 lv.queue_free()）；对象池测试节点
  靠 ObjectPool.drain() 清理。
- **收尾**：main.gd 自检 102→104（数据表锚点 + 槽位边界）；回归 35→36；
  README 5.41 测试报告节（含 36 入口回归矩阵）；记忆；提交。
- **阶段 8 完成状态**：8.1 存档（a12f99f）/ 8.2 设置（a12f99f）/ 8.3 性能（3c5f7c0）/
  8.4 平衡（681becc）/ 8.5 回归（本提交）全部落地。
- **下一阶段**：9.1 导出 Windows 可执行（纯本地不发行，D7）→ 9.2 本地自测（需你参与）
  → 9.3 崩溃容错 → 9.4 手感迭代。

## 阶段 9.1/9.3 本地打包与稳定性（9.3 已提交；9.1 导出等模板）

- **9.1 导出前置**：主场景 res://scenes/main/main.tscn 已设；export_presets.cfg 已写
  （Windows Desktop / x86_64 / embed_pck=true / 输出 build/七傳說.exe）；
  导出模板缺失——TuxFamily 镜像 4.7.2 目录已空（404）、清华/山大 GitHub 镜像 404，
  GitHub release 直连可下但慢（约 14MB/分钟，需 ~1GB）。模板安装目标
  %APPDATA%\Godot\export_templates\4.7.2.stable\。
- **窗口化启动实测发现 2 个真实 Bug（headless 验证一直没暴露）**：
  ① AudioManager.play 在 main._ready 自检阶段同步 add_child → 父节点树忙失败 +
  Playback only in tree（×3）→ 修复：add_child.call_deferred + player.ready.connect(player.play)；
  ② main.gd 自检 `%g` C 风格格式 GDScript 不支持 → 改 %d。修复后窗口化 stderr 0 行。
  （注意：GDScript 的 % 格式化支持 %s/%d/%f/%v/%x/%o 和 %% 转义，**不支持 %g**。）
- **9.3 稳定性补丁**：scripts/core/game_log.gd（class_name GameLog 纯静态）——
  user://logs/game.log 时间戳落盘 + 2MB 轮转保留 7 份 + read_tail；
  main.gd 启动记 GameLog.info（引擎/模式）；verify_fix93 6 项（落盘/时间戳/尾部/
  存档损坏回滚/清理/轮转约定）全过；main.gd 自检 104→106；回归 36→37；
  boot_preview.gd|.tscn → build/boot_preview.png（主场景启动画面）验收通过；
  README 5.42。**注意**：PackedScene load 需要显式类型 var s: PackedScene = load(...)
  （`:=` 推断报 Cannot infer type）。
- **9.1 待办**：模板下载完成后解压安装 → --export-release 导出 → 验证 exe 双击启动
  → README 5.43 → 提交。

## 阶段 9.1 本地打包导出（已提交）

- **模板下载教训**：Godot 4.7.2 导出模板 1.22GB（GitHub release 资产）。TuxFamily
  镜像目录已空、ghfast.top 不通、gh-proxy.com 多连接被限流卡死（8 分片 47MB/min 后归零）、
  **最终 GitHub 直连 ~15-23MB/min + curl -C - 断点续传完成**（期间停滞 2 次，杀 curl 重启续传恢复；
  robust_download.py 带 120s 停滞检测自动重启，可复用）。
  注意 HEAD/GET Range 探测：gh-proxy Accept-Ranges: bytes 但实际限流。
- **导出坑**：① 导出 release 模板编译了 disable_path_overrides=yes → exe 不接受 --path，
  验证时直接 `七傳說.exe --headless -- --verify`；② **Godot 导出包默认排除 export_presets.cfg** →
  main.gd 自检「导出预设」项在发布态 false → 改为发布态自动通过（preset_91=="" 分支 true，
  开发态仍严格校验）；③ 模板安装目标 %APPDATA%\Godot\export_templates\4.7.2.stable\
  （含 version.txt "4.7.2.stable"），tpz 是 zip 直接 zipfile 解压，templates/ 目录拷入。
- **产物**：export_presets.cfg（Windows Desktop/x86_64/embed_pck=true）、
  build/七傳說.exe 110MB（git 忽略不入库，.gitignore 已配）、verify_export91 5/5、
  编辑器自检 108、导出 exe 自检 108、窗口化冒烟 8s 无闪退 + 日志落盘；
  回归 37→38；README 5.43。9.2 本地自测待用户双击 exe 试玩（需用户参与）。

## 阶段 9.2 本地完整流程自测（自动化部分已提交，玩家试玩待反馈）

- **verify_play92 7/7**（seed=20260917 固定）：新档（生命150/攻击12）→ 刷怪掉落
  staff_ashen Lv.5 → 换装攻击 12→25 → 存档 → 重开全字段一致。已加入回归 38→39。
- **坑**：① StatCalculator 生命键是 **max_hp**（不是 max_health）；② EquipmentInstance
  槽位字段是 **slot**（int，GameConstants.EquipSlot 枚举），不是 slot_name；
  ③ 装备底材 ID 是 sword_iron（非 iron_sword，弓类是 bow_longbow）；
  ④ GDScript `var x := a and b` 中 a/b 含 Variant（Dictionary.get）会「Cannot infer type」
  → 必须显式 `var x: bool = ...`；⑤ LootRoller roll 是随机的，断言要多次采样。
- **9.2 剩余**：玩家双击 build/七傳說.exe 实际试玩 → 手感反馈 → 9.4 迭代。
  自测清单：新档→刷怪→掉落→换装→变强→存档→重开。

## ⚠️ 主理人只读排查（2026-09-17 16:45）：发现「集成层缺失」—— 游戏实际不可玩

用户要求「先不要修改，先排查了解情况」。全程只读，未改动任何文件。

**结论：数据层 + 系统层做得很扎实，但缺整个集成层，双击 exe 打开的是自检面板不是游戏。**

**🔴 P0 集成层缺失（硬证据）**
- `SceneManager.SCENE_HUB = "res://scenes/main/hub.tscn"` → **全盘搜索 hub.tscn 不存在**；
  `change_to_hub()` 一旦调用必崩。
- `change_to_level()` 拼 `res://scenes/levels/%s.tscn` → **`game/scenes/levels/` 只有 .gitkeep**，
  20 关 JSON 的 `scene_path` 全部指向不存在的文件。
- 全项目**只有 7 个 .tscn**（damage_dummy / enemy_base / damage_number / pixel_burst /
  loot_drop / main / player），**没有关卡场景、没有据点场景、没有 UI 场景**。
- 主场景 `main.tscn` = 骨架自检面板；`main.gd` 头部自述「本脚本**不是游戏玩法**…
  阶段 2 会把本场景替换为主菜单 / 据点」——**这步从未发生**。
- 7 个 UI 面板（MainMenu/Stat/Equip/Talent/Forge/Result/Settings）**只在 `main.gd` 自检里
  `.new() != null`、以及 `ui_preview.gd` 截图工具里出现**，无任何游戏流程实例化。
- `change_to_hub` / `change_to_level` **全项目零调用点**（只有 scene_manager 自身定义）。
- 导出 exe 115 MB → 双击进自检面板。

**🟠 P1 文档与实现脱节**
- README 待办表 **#19 编号重复**（第 19 位 + #22 之后各一个）。
- README 第 1947 行「数据表」行**严重过期**：写 装备 42 / 怪物 8 / 关卡 6 / 技能 3，
  实际 **62 / 16 / 20 / 8**（与同一份 README 后面章节自相矛盾）。
- **6 条待办已完成却未销项**：#2（GDD 已 v1.8 同步）、#4（美术帧数已 v1.7 修）、
  #6（传奇特效现 31 ≥30）、#9（关卡已 20）、#10（scripts/items 已有 2 文件）、
  重复 #19（魔石 108 vs 106 已在 GDD v1.8 解决）。
- **任务清单 72 项只有 1 项打勾** —— 从未随进度更新。
- `deliverables/gstack/` **没有平衡报告与测试报告**；8.4/8.5 的「报告」只是 README 章节，
  不符「报告落盘 deliverables/gstack/」的约定。

**🟡 P2 卫生**
- `.vu/` **3.2 GB / 425 文件**（含 1.22 GB Godot 导出模板下载 + 字体下载 + 140+ 日志）；
  已 gitignore 但占盘巨大。
- `game/build/` 111 MB（exe + 32 张预览 PNG）；`game/.godot/` 5.8 MB。

**✅ 确认做得好的**
- 108 项骨架自检 + 36 个 verify 脚本全绿；数据计数与声明**完全一致**
  （装备 62 / 词缀 33 / 词缀池 10 / 怪物 16 / 关卡 20 / 技能 8 / 特效 31 / BOSS 2 / 掉落表 3）。
- GDD 数值落地准确（XP 180、魔石 106、方案 D 系数 1.08/1.23）。
- 待办表**诚实标注**了缺口（#7 关卡场景 / #8 hub / #13 副手底材 / #20 相机缩放）—— 没掩盖。

**根本原因：任务清单本身缺一条「集成 / 串联」任务。**
阶段 2–7 全部按「系统」切分（战斗、装备、局内、局外、内容、UI），
**没有任何一条写「把系统组装成一个可玩的关卡循环」**。所以每个系统都做完、都验证了，
但没有人负责把它们接起来。这是**计划层面的缺口**，不是执行偷懒。

**建议下一步（待用户放行）**
1. 补 3 个场景：`hub.tscn`（据点/主菜单入口）、`level.tscn`（关卡容器：LevelView + 玩家 +
   敌人生成 + HUD）、UI 流接线（7 面板串成 主菜单→据点→选关→关卡→结算）。
2. 任务清单新增「集成里程碑」任务，并更新 72 项勾选。
3. 修 README 待办表（编号重复 / 过期数据表行 / 6 条销项）。
4. 清理 `.vu/`（3.2 GB）。

**审计报告已落盘**：`deliverables/gstack/integration-audit-2026-09-17.md`（v1.0）
—— 16 项发现（🔴8 / 🟠5 / 🟡3 + 🟢4 良好项）+ 8 条行动清单 + 「能玩的 demo」最小新增清单。
**团队状态**：5 位成员全部因 429 频率限制失败（额度 2026-09-18 10:58 UTC+8 恢复），
本轮审计由主理人独立完成 + godot-engineer-2 失败前交付的只读调研。

**补充核验（本轮新增）**：
- `FORGE_STONE_COST = [3,3,3,7,7,7,13,13,25,25]`（合计 **106** ✓，`FORGE_STONE_COST_EXT=[30,30]`
  用于神话 +11/+12）→ README 待办 #19（魔石 108 明细缺口）**已可销项**。
- README 的 6 处「方案 B / 6.09 / 1.45」**全为合法引用**（4 处是「D2 方案 B」= 死亡机制决策，
  与数值方案 B 无关；1 处已改写为「✅ 已拍板方案 D，6.09/1.45 已被否决」）—— 不是残留。
- GDD **v1.8 已落盘**：269 只 / 区间 192–347 / BOSS 90–120s / XP 180 / 魔石 106（分档
  +9~+10 各 25）/ 单局魔石 59 / 经验 4,500–7,200 ×1.79 / 第 84 行改「✅ 已核对」/ 风险表第 10 项统一。
  产品评审员另修正旧经验表在 L30+ 的**公式误差**（原整体 ×0.973 缩放 → 改逐点实算 `180×L^1.6`），
  并新增「关/级实算节奏表」，把长尾曲线列为新风险。
